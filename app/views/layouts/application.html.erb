<!DOCTYPE html>
<html>
<head>
  <title>Ludomanager</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>  
  <div class="navbar navbar-fixed-top">
        <div class="navbar-inner">
          <div class="container-fluid">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </a>
            <%= link_to "Ludomanager", root_path, :class => "brand" %>
            <div class="nav-collapse">
              <ul class="nav">
                <li class="active"><%= link_to "Games", games_path %></li>
                <% if account_signed_in? %>
                  <li><%= link_to "My Games", account_account_games_path(current_account) %></li>
                <% end -%>
                <li><a href="#contact">Contact</a></li>
              </ul>
              <p class="navbar-text pull-right">
              <% if account_signed_in? %>
                Logged in as <%= link_to current_account.email, account_path(current_account) %> | 
                <%= link_to "Log Out", destroy_account_session_path, :method => :delete %> 
              <% else %>
                <%= link_to "Sign In", new_account_session_path %>
              <% end %>
              </p>
            </div><!--/.nav-collapse -->
          </div>
        </div>
      </div>

      <div class="container-fluid">
        <div class="row-fluid">
          <div class="span3">
            <div class="well sidebar-nav">
              <ul class="nav nav-list">
                <%= yield :navbar %>
              </ul>
            </div><!--/.well -->
          </div><!--/span-->
          <div class="span9">
            <%= yield %>
          </div>
          
        </div><!--/row-->

        <hr>

        <footer>
          <p>&copy; Company 2012</p>
        </footer>

      </div><!--/.fluid-container-->
  </div>
</body>
</html>
